# GIT_CONVENTION.md — Git 커밋 컨벤션

> 이 파일은 모든 Repository에 복사하여 사용한다.
> Claude Code는 커밋 생성 시 이 규칙을 반드시 따른다.

---

## 기본 원칙

- 커밋은 **기능 단위**로 작성한다 (하나의 커밋 = 하나의 논리적 변경)
- 메시지는 **간결하게** 작성하되, 타입 분류가 명확히 보여야 한다
- 커밋 시 **`-s` 옵션을 항상 사용**한다 (`Signed-off-by` 자동 추가)

---

## 커밋 명령 형식

```bash
git commit -s -m "[타입] 제목"
```

---

## 타입 분류표

| 타입 | 의미 | 사용 예 |
|---|---|---|
| `feat` | 새 기능 추가 | 새 메뉴, 새 클래스, 새 API |
| `fix` | 버그 수정 | 잘못된 로직, 크래시, 오탈자 수정 |
| `refactor` | 동작 변경 없는 코드 구조 개선 | 함수 분리, 네이밍 변경 |
| `test` | 테스트 추가 또는 수정 | gtest 케이스 추가 |
| `docs` | 문서만 수정 | CLAUDE.md, PRD.md, README.md |
| `chore` | 빌드 설정, 패키지, 기타 | .vcxproj 설정, nlohmann 추가 |
| `style` | 포맷·공백 등 코드 의미 무관한 변경 | 들여쓰기, 줄바꿈 정리 |

---

## 커밋 메시지 작성 규칙

### 형식
```
[타입] 제목 (50자 이내 권장)
```

### 제목 작성 요령
- 명사형 또는 동사 원형으로 작성 (`구현`, `추가`, `수정`, `제거`)
- 마침표 없음
- 구체적으로, 하지만 짧게

---

## 자주 쓰는 커밋 패턴 (복사용)

```bash
# 기능 추가
git commit -s -m "[feat] "

# 버그 수정
git commit -s -m "[fix] "

# 테스트 추가
git commit -s -m "[test] "

# 문서 수정
git commit -s -m "[docs] "

# 빌드/설정 변경
git commit -s -m "[chore] "
```

---

## Claude Code 사용 시 주의

Claude가 커밋을 생성할 때는:
1. 반드시 `-s` 옵션을 포함한다
2. 타입 분류표에서 가장 적합한 타입 하나만 선택한다
3. 제목은 간결하게, 타입이 명확히 보이도록 작성한다
4. 본문(body)도 간략하게 작성한다. 변경된 내용을 `-` 기호로 나열한다.

```
[feat] OrderController 승인 로직 추가

- 재고 충분 시 CONFIRMED 전환
- 재고 부족 시 PRODUCING 전환 및 생산큐 등록
```
